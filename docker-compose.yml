version: "3.9"

services:
  # ── Qdrant Vector Database ──
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant
    restart: unless-stopped
    ports:
      - "0.0.0.0:6333:6333"   # REST API
      - "0.0.0.0:6334:6334"   # gRPC
    volumes:
      - ./qdrant/data:/qdrant/storage
    environment:
      QDRANT__SERVICE__API_KEY: "${QDRANT_API_KEY}"
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'echo > /dev/tcp/localhost/6333'"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ── Tool / Skill API (FastAPI + FastEmbed) ──
  tool-api:
    build:
      context: ./tool-api
      dockerfile: Dockerfile
    container_name: tool-api
    restart: unless-stopped
    ports:
      - "0.0.0.0:8100:8000"
    environment:
      QDRANT_HOST: qdrant
      QDRANT_PORT: 6333
      QDRANT_API_KEY: "${QDRANT_API_KEY}"
    depends_on:
      qdrant:
        condition: service_healthy
    volumes:
      - ./tool-api:/app
      - ./data:/data
      - fastembed-cache:/root/.cache/fastembed

  # ── OpenWebUI ──
  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    container_name: open-webui
    restart: unless-stopped
    ports:
      - "0.0.0.0:3000:8080"
    environment:
      WEBUI_AUTH: "true"
      ENABLE_SIGNUP: "true"
      # Codex proxy (맥 로컬 :8200)
      OPENAI_API_BASE_URLS: "http://host.docker.internal:8200/v1;http://host.docker.internal:8201/v1"
      OPENAI_API_KEYS: "dummy-codex;dummy-claude"
      CODE_INTERPRETER_PROMPT_TEMPLATE: |-
        #### Code Interpreter

        You have access to a Python code interpreter via: `<code_interpreter type="code" lang="python"></code_interpreter>`

        - When you need Python execution, put only executable Python code inside the XML tags so the tool can run it.
        - Do NOT use triple backticks inside the XML tags.
        - Always print meaningful outputs.
        - After the interpreter output, continue with a normal chat response outside the XML tags and do not end the user-facing answer as only a code block.
    volumes:
      - open-webui-data:/app/backend/data
      - ./open-webui-runtime-patches/middleware.py:/app/backend/open_webui/utils/middleware.py:ro
    depends_on:
      - tool-api

volumes:
  open-webui-data:
  fastembed-cache:
